
[target]
runner = "xtensa-esp32-elf-gdb -q -x openocd.gdb"


[build]
rustflags = [
  # TODO we can probably use lld, if we can figure out how to build it!
  "-C", "linker=xtensa-esp32-elf-ld",
  "-C", "link-arg=-Tlink.x",
]
target = "xtensa-esp32-none-elf"
